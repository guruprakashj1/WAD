Root Node : is the top most node in the XML structure. 
Element Node : Start and an End tag.
Attribute Node : are present within a start tag and name - value pair. 
Text Node : are placeholder for the text data

Comment Nodes: 

$doc = new DOMDocument();
